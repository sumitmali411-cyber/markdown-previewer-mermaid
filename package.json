{
  "name": "markdown-mermaid-viewer",
  "version": "1.0.0",
  "description": "Playwright test suite for Markdown + Mermaid Viewer",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
